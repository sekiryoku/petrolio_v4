document.addEventListener("DOMContentLoaded", () => { let e = 15; console.log("Initial current_places:", e); let t = document.querySelectorAll(".random"), l = () => { t.forEach(t => { t.textContent = `Current places: ${e}` }) }; l(); let r = () => { let t = 1e3 * Math.floor(26 * Math.random() + 15); setTimeout(() => { e > 1 && (e -= 1, console.log("Decreased current_places:", e), l(), r()) }, t) }; r() });